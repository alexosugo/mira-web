# Gate 1 selection

Selected production direction: **Disciplined**.

Rationale:

- preserves the approved Sellogram concept and uppercase `S` leadership;
- improves the double-`l` rhythm;
- tightens `ogram` into a more coherent optical unit;
- controls the terminal `m` better than the source and Faithful candidate;
- retains more fidelity to the chosen concept than Characterful.

The authoritative production copy is `master/sellogram.svg`.

Small-size evaluation must render from the SVG master through a vector rasterizer. Do not use the thresholded binary-mask review path for final quality judgement.
